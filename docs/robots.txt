# robots.txt — Glass Cannon Project
#
# Pre-launch policy: the data here is not yet verified, so AI-TRAINING crawlers
# are asked to stay out. Search engines are deliberately NOT blocked: they must be
# able to fetch each page to see its <meta name="robots" content="noindex">
# (blocking them here would hide that tag and can leave bare URLs listed anyway).
# This file is a request that well-behaved crawlers honor, not enforcement.
#
# LAUNCH CHECKLIST — to open the site to search and AI datasets:
#   1. public-site/mkdocs.yml: set `extra.discoverable: true` (drops the noindex tag)
#   2. this file: delete every "Disallow: /" block below (keep the trailing
#      "User-agent: *" block, or replace it with a Sitemap: line)
#   3. push to main (publish_public.yml runs both gates and deploys)

User-agent: GPTBot
User-agent: ClaudeBot
User-agent: anthropic-ai
User-agent: Claude-Web
User-agent: CCBot
User-agent: Google-Extended
User-agent: Applebot-Extended
User-agent: PerplexityBot
User-agent: Bytespider
User-agent: Meta-ExternalAgent
User-agent: Amazonbot
User-agent: cohere-ai
User-agent: Diffbot
User-agent: ImagesiftBot
User-agent: Omgilibot
Disallow: /

User-agent: *
Allow: /
